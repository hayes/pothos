{
  "id": "plugin-tracing-lifecycle",
  "title": "Observe resolver completion",
  "description": "Run the mutation to compare successful, failing, and untraced resolvers. The final field returns the trace events in order.",
  "category": "plugins",
  "relatedDocs": ["/docs/plugins/tracing"],
  "defaultActiveFile": "schema.ts"
}
