{
  "id": "plugin-relay-pagination",
  "title": "Page through a deterministic connection",
  "description": "Run the first page, copy its endCursor into the second page variables, and run again. Compare backward pagination and the empty page. Change first from 2 to 1 and follow the new cursor.",
  "category": "plugins",
  "relatedDocs": ["/docs/plugins/relay"],
  "defaultActiveFile": "schema.ts"
}
