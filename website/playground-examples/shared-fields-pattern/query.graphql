query SharedFieldsExample {
  # Both Team and Player expose id/createdAt via the addAuditFields
  # helper, not by redefining the fields on each type.
  teams {
    id
    createdAt
    name
  }

  players {
    id
    createdAt
    name
  }
}
