query Search {
  search(term: "shire") {
    __typename
    ... on Character {
      id
      name
    }
    ... on Location {
      name
      terrain
    }
    ... on Quote {
      text
    }
  }
}
