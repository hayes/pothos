# Change userId to 2 in Context to see the other viewer and their drafts.
query Viewer {
  me {
    __typename
    email
    user {
      fullName
    }
    drafts {
      title
    }
    ... on EditorViewer {
      canReviewSubmissions
    }
  }
}
