mutation {
  register(input: { name: "Li", email: "invalid" }) {
    __typename
    ... on MutationRegisterSuccess {
      data
    }
    ... on InputValidationError {
      issues {
        message
        path
      }
    }
  }
}
