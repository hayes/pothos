query Pages {
  posts(first: 2) {
    totalCount
    nodes {
      title
      media {
        url
        uploadedBy {
          name
        }
      }
    }
    pageInfo {
      endCursor
      hasNextPage
    }
  }
}
