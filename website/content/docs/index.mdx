---
title: Overview
description: Build fully type-safe GraphQL schemas in TypeScript, without writing your types twice.
---

import { Callout } from 'fumadocs-ui/components/callout';

![Pothos](/assets/logo-name-auto.svg)

Pothos is a library for building GraphQL schemas in TypeScript. You define your types, fields, and resolvers with a builder, and the whole schema is fully type-safe without writing your types twice. The builder keeps track of your data's types wherever they are used, so there is no SDL to keep in sync and no code-generation step.

`builder.toSchema()` hands you a standard graphql-js `GraphQLSchema` at startup, so any server — [GraphQL Yoga](https://the-guild.dev/graphql/yoga-server), Apollo — can run it, and your resolvers stay ordinary functions. Pothos does its work in the type system and while the schema is being built; resolvers themselves run as plain functions. Core depends only on `graphql`. Everything past it — Relay, scope-auth, errors, validation, dataloaders, Prisma, Drizzle, federation — is a plugin that extends the same builder.

<include cwd lang="typescript" meta='playground example="getting-started-first-schema-step-2"'>playground-examples/getting-started-first-schema/step-2/schema.ts#schema</include>

Open the example in the playground to run it, edit the resolver, and watch the SDL update. For the full narrative — how Pothos compares to schema-first tools, the mental model the rest of the guide assumes, and when to reach for it — read the [Introduction](./getting-started/introduction).

## How it fits together

The example above is the whole pipeline in miniature:

- The backing model (`{ id: string; name: string }`) is a plain TypeScript type describing the data. `builder.objectRef<T>()` creates a GraphQL type based on it.
- The `frodo` resolver needs to return that shape, and TypeScript will report an error if it doesn't.
- `builder.toSchema()` produces a normal graphql-js `GraphQLSchema` that works with any GraphQL server.

Plugins add methods to the same builder (`t.connection`, `authScopes`, `t.prismaField`) with the same type safety as the built-in ones.

## Explore the docs

<Cards>
  <Card
    title="Getting started"
    href="/docs/getting-started/introduction"
    description="Install Pothos and go from an empty project to a running GraphQL endpoint."
  />
  <Card
    title="Fundamentals"
    href="/docs/fundamentals/schema-builder"
    description="The core building blocks — objects, fields, resolvers, inputs, interfaces, unions, and scalars."
  />
  <Card
    title="Patterns"
    href="/docs/patterns/project-layout"
    description="Real-world recipes for structuring a schema — project layout, error handling, and circular references."
  />
  <Card
    title="Plugins"
    href="/docs/plugins"
    description="The full plugin catalog — auth, errors, validation, Relay, dataloaders, ORMs, and more."
  />
  <Card
    title="API reference"
    href="/docs/api/schema-builder"
    description="Method-by-method reference for the schema, field, arg, and input-field builders."
  />
  <Card
    title="Migrations"
    href="/docs/migrations/v4"
    description="Upgrade guides between major versions, plus the move from GiraphQL to Pothos."
  />
</Cards>

<Callout type="info">
  See also: [Introduction](./getting-started/introduction) for the mental model behind Pothos, or [Installation](./getting-started/installation) to start building.
</Callout>
