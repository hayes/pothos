---
title: Plugins
description: List of plugins for Pothos
---

{/*
  The plugin catalog at /docs/plugins is served by the app route
  `app/(docs)/docs/plugins/page.tsx`, which renders the rich, categorized
  `PluginsPage` from `components/plugins/plugins.ts`. Next.js resolves that
  static route ahead of this MDX catch-all, so nothing authored here would
  ever render — this file exists only so fumadocs keeps a clickable "Plugins"
  node in the sidebar that points at /docs/plugins. Do not re-add a card grid;
  edit `components/plugins/plugins.ts` (the single source of truth) instead.
*/}
