---
"@pothos/core": patch
---

Preserve literal types in synchronous and asynchronous field resolver results, including enum values and discriminated objects and lists, without requiring assertions or return annotations. Keep mutable backing properties and existing resolver return validation intact.
