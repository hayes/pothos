---
"@pothos/core": patch
---

Preserve literal types in synchronous field resolver results, including enum values and discriminated object lists, without requiring assertions or return annotations. Keep mutable backing properties and existing resolver return validation intact.
