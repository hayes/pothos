---
'@pothos/plugin-federation': patch
---

Remap output type references when replacing the federation query root, allowing fields and unions to reference conventional or custom query roots without duplicate types.
