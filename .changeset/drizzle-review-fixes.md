---
"@pothos/plugin-drizzle": patch
---

Pass the drizzle table, rather than its relational config, to connection helper `orderBy` callbacks
Expand `columns` selections that only exclude columns, instead of selecting nothing but the primary key
