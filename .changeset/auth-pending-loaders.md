---
"@pothos/plugin-scope-auth": patch
---

Consume pending scope-loader rejections when another loader throws synchronously, including nested scope maps, while preserving the synchronous authorization error.
